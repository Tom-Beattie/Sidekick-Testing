[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Vite/SPA history fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
